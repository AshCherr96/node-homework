import { defineConfig } from "@neon/config/v1";

// This project is deployed through the existing app/runtime setup and does not
// currently define any branch-specific Neon deployment overrides.
export default defineConfig({});
